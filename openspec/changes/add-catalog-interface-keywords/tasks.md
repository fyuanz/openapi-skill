# Tasks

## 1. 先建立失败的行为测试

- [x] 1.1 在 Java core 和 Node 中增加聚焦 catalog 的测试，覆盖无配置 keywords、summary/operationId 缺失、多 tag、无 tag、空文档、同名跨服务/文档接口、中文及特殊字符；运行对应测试并记录现实现缺失两层检索信息的失败。
- [x] 1.2 增加逐级相对链接、导航模板、来源文字隔离及输入排列稳定性测试；验证旧实现失败，断言不依赖无关 Markdown 排版。
- [x] 1.3 增加 Java 聚合旧服务树补充检索区、幂等性和事实保留测试，以及输出预算失败保留测试；确认失败原因对应新增行为而非环境问题。

## 2. 服务与两层目录生成

- [x] 2.1 基于现有操作索引及已有关键词实现每种语言共用的检索视图和短行渲染，明确排序、去重、method/path 保真和转义；使单服务行为测试由红转绿，保持 JSONL 格式不变。
- [x] 2.2 在 Node 项目和 Java 聚合的外层 catalog 按服务/文档/分组输出完整检索条目，保留服务 catalog 入口；通过多服务归属、覆盖数量和所有本地链接断言。
- [x] 2.3 为 Java 聚合输入的旧服务 catalog 幂等补充检索区，保留原有事实和版本接受范围；通过旧成员、重复装配及契约文件不变的测试。
- [x] 2.4 更新单服务、Node 项目和 Java 聚合 SKILL.md 的逐级查找、精确匹配、未命中及多候选说明；通过模板断言并确认来源自由文本未进入受信模板。

## 3. 集成验证与文档交付

- [x] 3.1 运行 `mvn -B clean install` 与 `openapi-skill-node` 下的 `npm test`，确认现有生成、验证及原子替换回归通过，并记录红到绿证据。
- [x] 3.2 用同一组脱敏输入比较 Java/Node 检索语义；生成多服务及大量接口样例，验证两层覆盖、分组数量、相对链接和稳定性，记录 catalog 字节数，并沿“上传文件”示例路径验证可到达正确操作。
- [x] 3.3 同步 README、Node 指南、产品设计及受影响的 `docs/codex/` 文档，将本提案状态更新为实际完成状态；验证文档路径和描述与最终输出一致，不将本次工作记录为注册表发布。
- [ ] 3.4 执行 `openspec validate add-catalog-interface-keywords --strict` 和 `git diff --check`，仅提交本变更文件并正常推送；记录检查及交付结果，遇远端阻塞时报告具体原因。

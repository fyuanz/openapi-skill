# Tasks

## 1. 先固定可观察诊断契约

- [x] 1.1 为 JSON 语法错误、重复键、LF/CRLF、多字节文本和不可定位错误增加失败测试，验证一基行列、`INVALID_JSON` 代码且不回显输入片段；确认测试在实现前为红。
- [x] 1.2 增加编译后 CLI 的子进程失败测试，覆盖默认单行 stderr、空 stdout、退出码 `1`，以及配置错误和未知异常的稳定阶段/代码；确认测试在实现前为红。
- [x] 1.3 增加 `--debug`、参数任意顺序、未知/重复/缺值参数、退出码 `2`、完整 cause 链、循环/深度上限测试；确认测试在实现前为红。
- [x] 1.4 增加敏感 URL 成分和文档秘密不进入默认 stderr 的测试，并增加已有 Skill 在 CLI 解析失败后逐字节不变的测试；确认测试在实现前为红。

## 2. 实现字段化诊断与位置报告

- [x] 2.1 实现内部诊断错误、上下文包装、阶段/代码归一化和安全单行格式化，运行聚焦测试验证文档级、配置级和未知错误均符合 spec。
- [x] 2.2 改造现有 JSON 扫描器以携带确定字符偏移和重复键信息，并统一换算一基行列；运行解析测试验证 LF、CRLF、多字节输入和无位置兜底。
- [x] 2.3 在读取、下载、解析、校验、生成和发布边界保留 `cause` 并补充诊断字段，运行现有生成/运行测试确认库入口仍只抛错且既有错误代码可识别。

## 3. 完成 CLI 行为

- [x] 3.1 扩展无依赖参数解析以支持一次 `--debug` 和一次 `--config <path>` 的任意顺序组合，并运行参数子进程测试验证成功、运行失败和用法失败退出码。
- [x] 3.2 实现默认诊断渲染及有循环检测和深度上限的 debug stack/cause 渲染，运行 CLI 子进程测试验证输出顺序、通道隔离和有界终止。
- [x] 3.3 运行原子发布与敏感信息测试，验证默认模式不披露 URL 凭证/查询/片段或文档正文，解析失败不改变已有 Skill。

## 4. 文档、项目记录与完整验证

- [x] 4.1 更新 Node 中英文 README，说明默认诊断格式、`--debug` 用法与调试输出可能包含敏感来源细节；通过针对性检索验证两种语言的命令和安全提示一致。
- [x] 4.2 更新 `docs/codex/TASKS.md` 的状态和红到绿证据，并在实现确认技术取舍后更新 `docs/codex/DECISIONS.md`；运行 `git diff --check` 验证文档格式。
- [x] 4.3 运行 `cd openapi-skill-node && npm test`、必要的本地 `npm pack --dry-run` 以及 `openspec validate "improve-node-cli-diagnostics"`，记录通过数量与任何无法运行的检查。
- [x] 4.4 按仓库里程碑规则提交本 change 的实现、测试、规格和文档；远程推送因网络不可用而由用户明确改为稍后手动执行，验证本地提交未混入无关文件。

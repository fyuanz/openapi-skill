# Tasks

## 1. 配置契约与路径校验

- [x] 1.1 先在 `project-config.test.mjs` 增加字符串兼容、默认单目标、1 至 8 个数组目标、相对/绝对路径顺序的失败测试，并以测试首次失败证明当前实现不支持数组
- [x] 1.2 增加空数组、超限、非字符串、空白、文件系统根目录、平台等价重复和祖先/后代目标的失败测试，并验证错误发生在任何文档读取或输出写入之前
- [x] 1.3 将公开配置类型扩展为 `output?: string | string[]`，把解析结果规范化为有序 `outputs`，保留首目标 `output` 兼容字段，并运行配置测试验证 1.1 和 1.2 全部转绿

## 2. 多目标发布编排

- [x] 2.1 先在 `run.test.mjs` 增加两个目标得到相同完整 Skill、生成计数不随目标倍增、返回 `skillDirectories` 且 `skillDirectory` 指向首目标的失败测试
- [x] 2.2 增加三目标中间失败的集成测试，验证首、末目标更新，失败目标保持旧完整树或不存在，命令整体失败且不存在跨目录回滚
- [x] 2.3 实现对同一不可变文件集合按声明顺序尝试全部目标的发布编排和结构化目标结果错误，保持 `publishSkill()` 的单目标原子边界，并运行发布与运行测试验证全部转绿

## 3. CLI 与安全诊断

- [x] 3.1 先在 CLI/诊断测试中固定多目标成功目录列表、部分失败退出码、各目标状态以及不泄露 OpenAPI 文本和带凭据 URL 的预期，并确认测试在实现前失败
- [x] 3.2 扩展诊断归一化与 CLI 格式化以输出全部成功目录或安全的部分失败摘要，保留现有单目标消息含义，并运行 CLI 与诊断测试验证转绿

## 4. 版本和用户文档

- [x] 4.1 先将包元数据测试更新为期望 `2.1.2`，并增加中英文 README 必须包含字符串/数组示例、1 至 8 个目标限制和非整体原子性说明的检查，确认测试首次失败
- [x] 4.2 将 `package.json`、`package-lock.json` 和相关版本断言更新为 `2.1.2`，更新 Node 中英文 README 的配置示例与失败语义，并运行元数据/文档测试验证转绿
- [x] 4.3 同步 `docs/codex/PROJECT_CONTEXT.md`、`MODULES.md`、`TASKS.md` 和 `DECISIONS.md` 中受影响的版本、输出能力、任务状态和设计决策，并通过针对 `2.1.1` 旧事实及新多输出描述的定向搜索核对一致性

## 5. 完整验证与交付

- [x] 5.1 在 `openapi-skill-node` 运行 `npm test`，确认配置、运行、发布、诊断和既有兼容测试全部通过
- [x] 5.2 运行 `npm pack --dry-run` 或等价打包检查，确认候选包版本为 `2.1.2` 且包含更新后的中英文文档和编译产物
- [x] 5.3 运行 `openspec validate support-multiple-output-directories --strict`，记录验证证据，更新项目任务完成状态，并按仓库里程碑规则提交且正常推送本次实现范围
